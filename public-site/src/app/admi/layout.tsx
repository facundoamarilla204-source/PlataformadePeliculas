import { AdminLayout } from '../../admin/components/layout/AdminLayout';

export default function AdmiLayout({ children }: { children: React.ReactNode }) {
  return <AdminLayout>{children}</AdminLayout>;
}
